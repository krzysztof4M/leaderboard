import React, { Component } from 'react';
import Form from './Form'

export default class Add extends Component {

    render(){
        return (
            <div>
                <Form />
            </div>
        )
    }
}